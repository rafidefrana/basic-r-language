#latihan list

#Buat daftar bernama math_list dengan elemen-elemen berikut: 
  #sebuah array dengan 3 dimensi dengan 2 elemen di masing-masing dimensi, diumpankan dengan data 1:2
  #sebuah vektor dengan elemen-elemen (3,9,81)
  #sebuah matriks dengan 2 baris dan 2 kolom dengan elemen (9,0,9,0)
#Beri nama elemen-elemen pada math_list Anda "ThreeDArray", "Vektor", dan "Matriks"
#Tambahkan elemen keempat ke dalam daftar Anda - elemen karakter dengan teks: "SAYA BERHASIL!"
#Ubah nama elemen keempat tersebut menjadi "String". Pertahankan nama-nama lain agar tidak berubah.
#Periksa jumlah elemen di dalam math_list Anda. Gunakan fungsi R untuk melakukannya.
#Hapus elemen 'String' dari daftar Anda.
#Ekstrak vektor dari daftar Anda dan tetapkan ke objek math_vector.
#Ubah math_vector Anda menjadi sebuah Array. Pertahankan namanya agar tidak berubah.
#Ekstrak elemen kedua dari dimensi ketiga ThreeDArray - tetapkan ke objek new_matrix.
#Kalikan setiap elemen dari new_matrix dengan elemen kedua dari objek math_vector.


#larik = array